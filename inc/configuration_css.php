<?php
	if ( ! defined( 'ABSPATH' ) ) {
		exit; // Exit if accessed directly
	} ?>
    <section class="abrf-page" data-abrf-tab="#abrf_css_value">
        <div class="abrf-page-head">
            <h2>🎨 CSS Property Configuration</h2>
        </div>
        <div class="abrf-divider"></div>
        <p class="abrf-lead">The CSS Property section lets you restyle the booking section without touching any code. Change the brand colors, border radius and font sizes below and they apply to every RentalForge element. All values are set in pixels (PX units).</p>
        <div class="abrf-alert abrf-warn">💡 Note: this value is applicable only for the RentalForge booking section, not the whole site.</div>
        <div class="abrf-tbl-wrap">
            <table class="abrf-table">
                <thead><tr><th>Field / Option</th><th>What it does</th></tr></thead>
                <tbody>
                <tr><th>Base Color</th><td>Choose the standard base color.</td></tr>
                <tr><th>Alternate Color</th><td>By choosing the default theme alternate color, the text color will be used if the backdrop color is Base Color, or alternately.</td></tr>
                <tr><th>Default Color</th><td>Select the default text color.</td></tr>
                <tr><th>Default Border Radios</th><td>Type the default border radius (in PX units).</td></tr>
                <tr><th>Default Font Size</th><td>Enter the default font size (in PX units).</td></tr>
                <tr><th>Font Size h1</th><td>Enter the h1 font size (in PX units). Default: <code class="abrf-chip">35px</code></td></tr>
                <tr><th>Font Size h2</th><td>Enter the h2 font size (in PX units). Default: <code class="abrf-chip">30px</code></td></tr>
                <tr><th>Font Size h3</th><td>Enter the h3 font size (in PX units). Default: <code class="abrf-chip">25px</code></td></tr>
                <tr><th>Font Size h4</th><td>Enter the h4 font size (in PX units). Default: <code class="abrf-chip">20px</code></td></tr>
                <tr><th>Font Size h5</th><td>Enter the h5 font size (in PX units). Default: <code class="abrf-chip">17px</code></td></tr>
                <tr><th>Font Size h6</th><td>Enter the h6 font size (in PX units). Default: <code class="abrf-chip">15px</code></td></tr>
                <tr><th>Label Font Size</th><td>Enter the label font size (in PX units). Default: <code class="abrf-chip">14px</code></td></tr>
                <tr><th>Button Font Size</th><td>Enter the button font size (in PX units). Default: <code class="abrf-chip">13px</code></td></tr>
                <tr><th>Button Text Color</th><td>Select the button text color.</td></tr>
                <tr><th>Button Background Color</th><td>Select the button background color.</td></tr>
                <tr><th>Warning Color</th><td>Select the warning color.</td></tr>
                <tr><th>Section Background color</th><td>Here you can add the section background color.</td></tr>
                </tbody>
            </table>
        </div>
    </section>
<?php