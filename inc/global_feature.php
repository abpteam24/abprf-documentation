<?php
	if ( ! defined( 'ABSPATH' ) ) {
		exit; // Exit if accessed directly
	} ?>
    <section class="abrf-page" data-abrf-tab="#abrf_feature_global">
        <div class="abrf-page-head">
            <h2>🔗 RentalForge – Global Feature Configuration</h2>
        </div>
        <div class="abrf-divider"></div>
        <p class="abrf-lead">Features are the highlighted points you want customers to see about your items (e.g. "Free cancellation", "Air conditioning", "24/7 support"). Creating them globally lets you reuse the same features on every rental item.</p>

        <div class="abrf-tbl-wrap">
            <table class="abrf-table">
                <thead>
                    <tr><th>Field / Option</th><th>What it does</th></tr>
                </thead>
                <tbody>
                    <tr><th>Feature Title</th><td>Give a short name to the feature, e.g. "Free Cancellation".</td></tr>
                    <tr><th>Icon</th><td>Optionally choose an icon that will be displayed next to the feature.</td></tr>
                </tbody>
            </table>
        </div>

        <ol class="abrf-steps">
            <li>Type Your Feature Name here.</li>
            <li>Click to Save Feature.</li>
            <li>The feature appears on items that inherit the global feature list.</li>
        </ol>
    </section>
<?php